{
  "name": "payment-backend-vercel",
  "version": "1.0.0",
  "description": "Payment backend for Vercel with Midtrans and Firebase integration",
  "type": "module",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "build": "echo 'Vercel will handle the build'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}